<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2006 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>escriba aquí el nombre para mostrar</displayName>
  <description>escriba aquí la descripción</description>
  <resources>
    <stringTable>
      <string id="WinMSG_NoAutoStartWindowsMsg_Comp">No ejecutar automáticamente Windows Messenger al inicio</string>
      <string id="WinMSG_NoAutoStartWindowsMsg_Help">Esta configuración de directiva impide que Windows Messenger se ejecute automáticamente al iniciarse una sesión. 

Si habilita esta configuración de directiva, no se carga Windows Messenger automáticamente cuando el usuario inicia una sesión.

Si deshabilita o no define esta configuración de directiva, se carga Windows Messenger automáticamente al iniciarse una sesión.

Nota: esta configuración de directiva solo impide que Windows Messenger se ejecute desde el principio. Si el usuario invoca y usa Windows Messenger desde ese momento, se cargará la aplicación.

Además, el usuario puede configurar este comportamiento en la pestaña Preferencias del menú Herramientas de la interfaz de usuario de Windows Messenger.

Nota: si no desea que los usuarios usen Windows Messenger, habilite la configuración de directiva "No permitir que se ejecute Windows Messenger".

Nota: esta configuración de directiva está disponible tanto en la Configuración del equipo como en la Configuración de usuario. Si ambas están presentes, la versión en el equipo de esta configuración de directiva tiene prioridad.</string>
      <string id="WinMSG_NoAutoStartWindowsMsg_User">No ejecutar automáticamente Windows Messenger al inicio</string>
      <string id="WinMSG_NoWindowsMsg_Comp">No permitir que se ejecute Windows Messenger</string>
      <string id="WinMSG_NoWindowsMsg_Help">Esta configuración de directiva le permite evitar que Windows Messenger se ejecute.

Si habilita esta configuración de directiva, no se ejecuta Windows Messenger.

Si deshabilita o no define esta configuración de directiva, se puede usar Windows Messenger.

Nota: si habilita esta configuración de directiva, Asistencia remota tampoco puede usar Windows Messenger.

Nota: esta configuración de directiva está disponible tanto en la Configuración del equipo como en la Configuración de usuario. Si ambas están presentes, la versión en el equipo de esta configuración de directiva tiene prioridad.</string>
      <string id="WinMSG_NoWindowsMsg_User">No permitir que se ejecute Windows Messenger</string>
      <string id="WinMSG_WindowsMsg">Windows Messenger</string>
    </stringTable>
  </resources>
</policyDefinitionResources>
