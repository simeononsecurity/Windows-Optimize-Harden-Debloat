<?xml version="1.0" encoding="utf-8"?>
<!--  (c) 2012 Microsoft Corporation  -->
<policyDefinitionResources xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" revision="1.0" schemaVersion="1.0" xmlns="http://schemas.microsoft.com/GroupPolicy/2006/07/PolicyDefinitions">
  <displayName>escriba aquí el nombre para mostrar</displayName>
  <description>escriba aquí la descripción</description>
  <resources>
    <stringTable>
      <!-- General -->
      <string id="SettingSyncCategory">Sincronizar la configuración</string>

      <!-- Main policy -->
      <string id="DisableSettingSync">No sincronizar</string>
      <string id="DisableSettingSync_Help">Impedir la sincronización hacia y desde este equipo.  Desactiva y deshabilita la opción "sincronizar la configuración" en la página "sincronizar la configuración" de Configuración de PC.

Si habilita esta configuración de directiva, se desactivará "sincronizar la configuración" y no se sincronizará ninguno de los grupos de "sincronizar la configuración" en este equipo.

Use la opción "Permitir a los usuarios activar la sincronización" para que la sincronización esté desactivada de forma predeterminada, pero no deshabilitada.

Si deshabilita o no establece esta configuración, "sincronizar la configuración" estará activada de forma predeterminada y el usuario podrá configurarla.</string>

      <!-- Application Settings -->
      <string id="DisableApplicationSettingSync">No sincronizar la configuración de aplicaciones</string>
      <string id="DisableApplicationSettingSync_Help">Impedir que el grupo "configuración de aplicaciones" se sincronice hacia y desde este equipo.  Desactiva y deshabilita el grupo "configuración de aplicaciones" en la página "sincronizar la configuración" de Configuración de PC.

Si habilita esta configuración de directiva, el grupo "configuración de aplicaciones" no se sincronizará.

Use la opción "Permitir a los usuarios activar la sincronización de la configuración de aplicaciones" para que la sincronización esté desactivada de forma predeterminada, pero no deshabilitada.

Si deshabilita o no establece esta configuración, la sincronización de la "configuración de aplicaciones" estará activada de forma predeterminada y el usuario podrá configurarla.</string>

      <!-- Credentials -->
      <string id="DisableCredentialsSettingSync">No sincronizar contraseñas</string>
      <string id="DisableCredentialsSettingSync_Help">Impedir que el grupo "contraseñas" se sincronice hacia y desde este equipo.  Desactiva y deshabilita el grupo "contraseñas" en la página "sincronizar la configuración" de Configuración de PC.

Si habilita esta configuración de directiva, el grupo "contraseñas" no se sincronizará.

Use la opción "Permitir a los usuarios activar la sincronización de las contraseñas" para que la sincronización esté desactivada de forma predeterminada, pero no deshabilitada.

Si deshabilita o no establece esta configuración, la sincronización de las "contraseñas" estará activada de forma predeterminada y el usuario podrá configurarla.</string>

      <!-- Personalization -->
      <string id="DisablePersonalizationSettingSync">No sincronizar personalización</string>
      <string id="DisablePersonalizationSettingSync_Help">Impedir que el grupo "personalización" se sincronice hacia y desde este equipo.  Desactiva y deshabilita el grupo "personalización" en la página "sincronizar la configuración" de Configuración de PC.

Si habilita esta configuración de directiva, el grupo "personalización" no se sincronizará.

Use la opción "Permitir a los usuarios activar la sincronización de la personalización" para que la sincronización esté desactivada de forma predeterminada, pero no deshabilitada.

Si deshabilita o no establece esta configuración, la sincronización de la "personalización" estará activada de forma predeterminada y el usuario podrá configurarla.</string>

      <!-- AppSync -->
      <string id="DisableAppSyncSettingSync">No sincronizar aplicaciones</string>
      <string id="DisableAppSyncSettingSync_Help">
        
        Impedir que el grupo "AppSync" se sincronice hacia y desde este equipo.  Desactiva y deshabilita el grupo "AppSync" en la página "sincronizar la configuración" de Configuración de PC.

Si habilita esta configuración de directiva, el grupo "AppSync" no se sincronizará.

Use la opción "Permitir a los usuarios activar la sincronización de aplicaciones" para que la sincronización esté desactivada de forma predeterminada, pero no deshabilitada.

Si deshabilita o no establece esta configuración, la sincronización del grupo "AppSync" estará activada de forma predeterminada y el usuario podrá configurarla.
      
      </string>

      <!-- Windows -->
      <string id="DisableWindowsSettingSync">No sincronizar otra configuración de Windows</string>
      <string id="DisableWindowsSettingSync_Help">Impedir que el grupo "otra configuración de Windows" se sincronice hacia y desde este equipo.  Desactiva y deshabilita el grupo "otra configuración de Windows" en la página "sincronizar la configuración" de Configuración de PC.

Si habilita esta configuración de directiva, el grupo "otra configuración de Windows" no se sincronizará.

Use la opción "Permitir a los usuarios activar la sincronización de otra configuración de Windows" para que la sincronización esté desactivada de forma predeterminada, pero no deshabilitada.

Si deshabilita o no establece esta configuración, la sincronización de "otra configuración de Windows" estará activada de forma predeterminada y el usuario podrá configurarla.</string>

      <!-- Desktop Theme -->
      <string id="DisableDesktopThemeSettingSync">No sincronizar la personalización del escritorio</string>
      <string id="DisableDesktopThemeSettingSync_Help">Impedir que el grupo "personalización del escritorio" se sincronice hacia y desde este equipo.  Desactiva y deshabilita el grupo "personalización del escritorio" en la página "sincronizar la configuración" de Configuración de PC.

Si habilita esta configuración de directiva, el grupo "personalización de escritorio" no se sincronizará.

Use la opción "Permitir a los usuarios activar la sincronización de la personalización de escritorio" para que la sincronización esté desactivada de forma predeterminada, pero no deshabilitada.

Si deshabilita o no establece esta configuración, la sincronización de la "personalización de escritorio" estará activada de forma predeterminada y el usuario podrá configurarla.
      
      </string>

      <!-- WebBrowser -->
      <string id="DisableWebBrowserSettingSync">No sincronizar la configuración del explorador</string>
      <string id="DisableWebBrowserSettingSync_Help">Impedir que el grupo "explorador" se sincronice hacia y desde este equipo.  Desactiva y deshabilita el grupo "explorador" en la página "Sincronizar tu configuración" de Configuración.  El grupo "explorador" contiene opciones de configuración e información como el historial y los favoritos.

Si habilita esta configuración de directiva, el grupo "explorador", incluyendo información como el historial y los favoritos, no se sincronizará.

Use la opción "Permitir a los usuarios activar la sincronización de las contraseñas" para que la sincronización esté desactivada de forma predeterminada, pero no deshabilitada.

Si deshabilita o no establece esta configuración, la sincronización del grupo "explorador" estará activada de forma predeterminada y el usuario podrá configurarla.
      
      </string>

      <!-- Paid Networks -->
      <string id="DisableSyncOnPaidNetwork">No sincronizar con conexiones de uso medido</string>
      <string id="DisableSyncOnPaidNetwork_Help">Impedir la sincronización hacia y desde este equipo con conexiones de Internet de uso medido.  Desactiva y deshabilita la opción "sincronizar la configuración con conexiones de uso medido" en la página "sincronizar la configuración" de Configuración de PC.

Si habilita esta configuración de directiva, la sincronización con conexiones de uso medido estará desactivada y no se realizarán sincronizaciones cuando este equipo tenga una conexión de uso medido.

Si deshabilita o no establece esta configuración, el usuario podrá configurar la sincronización con conexiones de uso medido.
      
      </string>

      <!-- StartLayout -->
      <string id="DisableStartLayoutSettingSync">No sincronizar la configuración de Inicio</string>
      <string id="DisableStartLayoutSettingSync_Help">
        
        Impedir que el grupo "distribución de la pantalla Inicio" se sincronice hacia y desde este equipo. Desactiva y deshabilita el grupo "distribución de la pantalla Inicio" en la página "sincronizar la configuración" de Configuración de PC.

        Si habilita esta configuración de directiva, el grupo "distribución de la pantalla Inicio" no se sincronizará.

        Use la opción "Permitir a los usuarios activar la sincronización de Inicio" para que la sincronización esté desactivada de forma predeterminada, pero no deshabilitada.

        Si deshabilita o no establece esta configuración, la sincronización del grupo "distribución de la pantalla Inicio" estará activada de forma predeterminada y el usuario podrá configurarla.
      
      </string>
    </stringTable>
    <presentationTable>

      <!-- Main policy -->
      <presentation id="DisableSettingSyncUserOverride">
        <checkBox refId="CheckBox_UserOverride">Permitir a los usuarios activar la sincronización.</checkBox>
      </presentation>

      <!-- Application Settings -->
      <presentation id="DisableApplicationSettingSyncUserOverride">
        <checkBox refId="CheckBox_UserOverride">Permitir a los usuarios activar la sincronización de "configuración de aplicaciones".</checkBox>
      </presentation>

      <!-- Credentials -->
      <presentation id="DisableCredentialsSettingSyncUserOverride">
        <checkBox refId="CheckBox_UserOverride">Permitir a los usuarios activar la sincronización de "contraseñas".</checkBox>
      </presentation>

      <!-- Personalization -->
      <presentation id="DisablePersonalizationSettingSyncUserOverride">
        <checkBox refId="CheckBox_UserOverride">Permitir a los usuarios activar la sincronización de "personalización".</checkBox>
      </presentation>

      <!-- AppSync -->
      <presentation id="DisableAppSyncSettingSyncUserOverride">
        <checkBox refId="CheckBox_UserOverride">Permitir a los usuarios activar la sincronización de "AppSync".</checkBox>
      </presentation>

      <!-- Windows -->
      <presentation id="DisableWindowsSettingSyncUserOverride">
        <checkBox refId="CheckBox_UserOverride">Permitir a los usuarios activar la sincronización de "otra configuración de Windows".</checkBox>
      </presentation>

      <!-- Desktop Theme -->
      <presentation id="DisableDesktopThemeSettingSyncUserOverride">
        <checkBox refId="CheckBox_UserOverride">Permitir a los usuarios activar la sincronización de "personalización de escritorio".</checkBox>
      </presentation>

      <!-- WebBrowser -->
      <presentation id="DisableWebBrowserSettingSyncUserOverride">
        <checkBox refId="CheckBox_UserOverride">Permitir a los usuarios activar la sincronización de "explorador".</checkBox>
      </presentation>
      
      <!-- StartLayout -->
      <presentation id="DisableStartLayoutSettingSyncUserOverride">
        <checkBox refId="CheckBox_UserOverride">Permitir a los usuarios activar la sincronización de "distribución de la pantalla Inicio".</checkBox>
      </presentation>

    </presentationTable>
  </resources>
</policyDefinitionResources>
